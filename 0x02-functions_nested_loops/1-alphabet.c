#include "main.h"
#include <stdio.h>
/**
 * print_alphabet - check the code
 *
 * Return Always 0
 */
void print_alphabet(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
